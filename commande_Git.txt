mkdir git
cd git
git init
ls -a
.git/config
cat > file1.txt
coucou
CTRL-C
git add file1.txt OU git add *
git commit -m "init"
git config --global user.name "ppoirot"
git config --global user.email ppoirot@iut.univ-paris.fr

echo "add" >> file1.txt
git add .
git commit -m "added"
git status
git log

REVERT DU GIT AU LOCAL
git checkout file1.txt

git diff

Basculement entre branche (par défaut sur master)
Git chekout NOMBRANCHE

git branch color
gedit
git add .
git commit

git checkout master
git branch size
gedit
git add .
git commit
